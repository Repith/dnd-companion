global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  # Additional services will be added here (backend, frontend) once they expose metrics endpoints.